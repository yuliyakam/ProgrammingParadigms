% Сумма списка из одного элемента = этому элементу
sum([X],X).
% Если убрать первый эл-т и найти сумму элементов в оставшемся списке, то общая
% сумма элементов будет равна сумме отброшенного элемента и сумме элементов в 
% оставшемся списке 
sum([H|T], Sum):- sum(T, Sum1), Sum is Sum1+H.

% Query
?- sum([10,2,5], Sum)